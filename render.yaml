services:
  - type: web
    name: quizWeb
    buildCommand: 
      pip install -r requirements.txt && python manage.py collectstatic --noinput && python manage.py migrate && python manage.py makemigrations

    startCommand: python manage_startup.py && gunicorn quizWeb.wsgi:application